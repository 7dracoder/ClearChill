# Supabase Configuration (OPTIONAL - only if you want to use Supabase instead of SQLite)
# Leave these blank to use SQLite (recommended for single-server deployment)
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_SERVICE_KEY=

# AI API Keys (REQUIRED)
GEMINI_API_KEY=your_gemini_api_key_here
K2_API_KEY=your_k2_api_key_here

# Image Generation (REQUIRED)
REPLICATE_API_TOKEN=your_replicate_token_here

# Server Configuration
HOST=0.0.0.0
PORT=8000
ENVIRONMENT=production

# Security (REQUIRED - generate with: openssl rand -hex 32)
SECRET_KEY=generate_with_openssl_rand_hex_32
ALLOWED_ORIGINS=https://yourdomain.com,http://YOUR_DROPLET_IP

# Database (SQLite - default, no configuration needed)
# Database file will be created as fridge.db in project root
# For custom location, set: FRIDGE_DB_PATH=/path/to/database.db

# Hardware Configuration (for Raspberry Pi)
API_BASE_URL=http://localhost:8000
API_TOKEN=your_jwt_token_here
LIGHT_SENSOR_PIN=17
LIGHT_THRESHOLD=0.5
CAPTURE_DELAY=2
WEBCAM_INDEX=0
